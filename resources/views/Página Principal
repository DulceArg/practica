<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Cupones Disponibles</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f4f4f4;
        }
        .card {
            border-radius: 10px;
            overflow: hidden;
        }
        .card img {
            height: 200px;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <h1 class="mb-4 text-center">Cupones Disponibles</h1>
        <div class="row">
            @forelse ($cupones as $cupon)
                <div class="col-md-4 mb-4">
                    <div class="card shadow-sm h-100">
                        <img src="{{ asset('storage/' . $cupon->imagen) }}" class="card-img-top" alt="Cupón">
                        <div class="card-body">
                            <h5 class="card-title">{{ $cupon->codigo }}</h5>
                            <p class="card-text">{{ $cupon->descripcion }}</p>
                            <p class="card-text"><strong>Descuento:</strong> {{ $cupon->descuento }}%</p>
                            <p class="card-text"><strong>Válido hasta:</strong> {{ \Carbon\Carbon::parse($cupon->fecha_expiracion)->format('d/m/Y') }}</p>
                        </div>
                    </div>
                </div>
            @empty
                <div class="col-12">
                    <div class="alert alert-info text-center">No hay cupones disponibles en este momento.</div>
                </div>
            @endforelse
        </div>
    </div>
</body>
</html>
